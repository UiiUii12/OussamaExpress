/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package ClientApp.app;

import ClientApp.list.LinkedList;

import static ClientApp.utilities.StringUtils.join;
import static ClientApp.utilities.StringUtils.split;
import static ClientApp.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
